# ZUMI（済） - 実装優先順位

> 関連: [Duty管理・記録・振り返り・アカウント](./03-duty-log-review-account.md) / [通知](./02-notifications.md) / [技術スタック・インフラ](./05-tech-stack.md)

## 7. 実装優先順位

| フェーズ | 内容 |
|---|---|
| 1 | 認証（better-auth + D1）、`packages/db` のスキーマとマイグレーション |
| 2 | Duty タブ（CRUD + DnD 並び替え + 繰り返しタイプ6種の設定UI） |
| 3 | 今日タブ（期日計算ロジック + 3セクション + ワンタップ押印） |
| 4 | 通知（Service Worker + notify Worker の Cron） |
| 5 | 振り返りタブ（週次サマリー + 履歴リスト + ヒートマップ） |
| 6 | **シェア画像生成（`workers-og`）** ← 獲得導線なので後回しにしない |
| 7 | 設定モーダル、オンボーディングのテンプレート選択 |
| 8 | LP |

フェーズ3の期日計算ロジックが実装上の山場。6タイプそれぞれの「今日が対象か」「催促が発生しているか」「週の残回数」を返す純粋関数として `packages/types` 側に切り出し、web と notify の双方から同じ実装を使う。ここがズレると「通知は来たのに画面に出ていない」が発生する。
