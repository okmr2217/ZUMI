# ZUMI（済） - MVP実装タスク一覧

> 関連: [Duty管理・記録・振り返り・アカウント](./03-duty-log-review-account.md) / [課金](./06-billing.md) / [実装優先順位](./07-roadmap.md)

## 9. MVP実装タスク一覧

`07-roadmap.md` のフェーズ構成を実装タスクレベルまで分解したもの。フェーズ0（基盤構築）・PWA対応・QA/リリース準備は `07-roadmap.md` の8フェーズには明記されていないが、実装上必須のため本ファイルで独立して洗い出す。

### フェーズ0: プロジェクト基盤構築

- [ ] Turborepo モノレポ初期化（`package.json` workspaces, `turbo.json`）
- [ ] `apps/web`（Next.js App Router + TypeScript）雛形作成
- [ ] `apps/notify`（Cron Trigger Worker）雛形作成
- [ ] `packages/db`（Drizzle スキーマ置き場）、`packages/types`（共有型）雛形作成
- [ ] `@opennextjs/cloudflare` アダプタ導入、`wrangler.toml`（web/notify 双方）設定
- [ ] Cloudflare D1 データベース作成、`database_id` を両 Worker の `wrangler.toml` に設定
- [ ] Tailwind CSS + shadcn/ui セットアップ
- [ ] ESLint / Prettier / TypeScript strict 設定、Turborepo 経由のスクリプト整備（lint/build/dev）
- [ ] Sentry 初期設定（web/notify 双方）
- [ ] CI（GitHub Actions）: lint/typecheck/build のパイプライン
- [ ] ローカル開発用 D1（`wrangler d1` ローカルモード）とシードスクリプト

### フェーズ1: 認証 + DBスキーマ

- [ ] `packages/db/schema.ts`: `userSettings` / `pushSubscriptions` / `duties` / `logs` テーブル定義
- [ ] better-auth 導入、Drizzle アダプタ設定（`user`/`session`/`account`/`verification`）
- [ ] `drizzle-kit generate` → `wrangler d1 migrations apply` のマイグレーションフロー確立
- [ ] メール+パスワードのサインアップ/ログイン/ログアウト API・画面
- [ ] メール認証（サインアップ時の確認メール送信・未認証時の利用制限）
- [ ] パスワードリセット（リセットメール送信 → トークン検証 → 再設定画面）
- [ ] 認証系メール送信基盤（Resend等。通知用メールとは別に、認証フローに最低限必要な送信経路を先に用意する）
- [ ] 退会機能（アカウント削除、関連データのカスケード削除確認）
- [ ] 認証ミドルウェア（`/app` 配下の保護）
- [ ] 利用規約・プライバシーポリシーへの同意チェックボックス（サインアップ時）

### フェーズ2: Duty タブ（CRUD）

- [ ] `ScheduleConfig` 型（`packages/types`）と6種のバリデーション（zod等）
- [ ] Duty 作成/編集/一時停止/アーカイブ API
- [ ] テンプレート一覧データ（プリセットDuty + アイコン）の定義
- [ ] Duty 一覧画面（カード表示、作成順、`sortOrder` は保持するがDnD UIはv1.1へ）
- [ ] 追加フロー: テンプレート選択 → 自由入力
- [ ] 編集フォーム: 名前・アイコン・繰り返しタイプ6種の設定UI・前夜通知トグル・メモ欄有無
- [ ] 無料プラン Duty 5件上限のチェックロジック（※課金導線は持たない。上限に達したら案内表示のみ、決済への誘導は行わない）

### フェーズ3: 今日タブ（期日計算ロジック）※実装上の山場

- [ ] `packages/types` に6タイプ分の純粋関数を実装（`isDueToday` / 催促判定 / 週の残回数 など）
- [ ] 上記ロジックのユニットテスト（境界値: 週またぎ、月末、隔週アンカー等）
- [ ] 今日タブ画面: 3セクション（今日／そろそろ／今週）+ 進捗バー（X/Y）
- [ ] ワンタップ押印（空円タップ→即DONE、モーダルなし）の API・楽観的UI更新
- [ ] 長押し詳細モーダル（対象日変更・スキップ・メモ）
- [ ] 完了済み折りたたみ（「済んだもの N件」）
- [ ] `(dutyId, targetDate)` ユニーク制約を用いた冪等な記録処理

### フェーズ4: 通知

- [ ] Service Worker（`apps/web/public/sw.js`）: push イベント受信・通知表示
- [ ] Web Push 購読フロー（VAPID鍵生成、許可ボタン、`pushSubscriptions`保存）
- [ ] `apps/notify` Cron Trigger（毎分実行）: タイムゾーン+通知時刻でユーザー抽出
- [ ] 朝の日次まとめ通知（対象ゼロなら送らない）
- [ ] 締め切りアラート（夜・未完了Duty）
- [ ] 週次サマリー通知（曜日・時刻設定を尊重）
- [ ] 期日不確定型の通知組み込み判定をフェーズ3の共有ロジックで実装（web/notify不整合防止）
- [ ] 通知設定UI（許可ボタン、朝/夜/週次のON/OFF・時刻、タイムゾーン）

### フェーズ5: 振り返りタブ

- [ ] 週次サマリー集計API（達成率算出）
- [ ] 週次サマリーカードUI
- [ ] 履歴リスト（日付グループ、アイコン・名前・DONE/SKIPバッジ・メモ、無限スクロール）
- [ ] 無料プラン履歴3ヶ月制限のクエリ・UI制御
- [ ] （月次カレンダーヒートマップはMVP対象外）

### フェーズ6: シェア画像生成（獲得導線のため優先度高）

- [ ] `workers-og`（Satori）セットアップ、`/og/w/:token` エンドポイント
- [ ] シェア用トークン発行・週次データ埋め込みロジック
- [ ] シェア画像テンプレート（標準テーマ、プリセットアイコンのみ）
- [ ] 振り返りタブからのシェア導線（Web Share API / 画像ダウンロード）

### フェーズ7: 設定モーダル・オンボーディング

- [ ] 設定モーダル: アカウント（パスワード変更・退会。メールアドレス変更はMVP対象外）
- [ ] 設定モーダル: 通知設定
- [ ] 設定モーダル: データ（エクスポートはMVP対象外）
- [ ] 設定モーダル: ヘルプ（繰り返し設定の説明）
- [ ] オンボーディングフロー: テンプレート選択 → 通知時刻設定 → 通知許可

### フェーズ8: LP

- [ ] `docs/08-lp-draft.md` を基にLPコンテンツ確定
- [ ] LP実装（カスタムCSS、ルート`/`に配置、`/app`と単一ドメイン共存）
- [ ] LPからサインアップ導線

### 横断: PWA対応

- [ ] `manifest.json`（アイコン、テーマカラー、`display: standalone`）
- [ ] Service Worker のキャッシュ戦略（最小限のオフライン対応）
- [ ] iOS「ホーム画面に追加」導線UI（iOS Web Push制約の案内）
- [ ] Lighthouse PWA監査・インストール可能性の確認

### 横断: 法務ドキュメント

- [ ] 利用規約の作成
- [ ] プライバシーポリシーの作成（取得データ、Sentryへの送信データの扱い等）
- [ ] 特定商取引法に基づく表記（課金開始前に必要。MVP時点では省略可）
- [ ] LP・サインアップ画面・フッターへの規約/ポリシーへの導線設置

### 横断: QA・リリース準備

- [ ] E2Eテスト（主要フロー: サインアップ→Duty作成→押印→通知→シェア）
- [ ] 本番D1・Cron Trigger・カスタムドメイン（`zumi.paritto.dev`）の疎通確認
- [ ] Cloudflare Workers デプロイパイプライン整備

## 課金（MVP対象外・v1.1以降）

MVP時点では**無料プランの制約ロジックのみ実装し、課金導線・決済連携は持たない**。課金機能が追加されるまでは、全ユーザーが無料プランの制約（Duty5件・履歴3ヶ月等）のもとで利用する（Pro向けの解放や決済誘導は行わない）。詳細は [06-billing.md](./06-billing.md) を参照。

決済連携が必要になった段階でのタスク（参考）:

- [ ] Stripe等の決済プロバイダ選定・連携
- [ ] Pro プラン管理（プラン種別カラム追加、Webhook処理での状態同期）
- [ ] Pro限定機能のフラグ制御（無制限Duty、履歴無制限、LINE通知、シェアテーマ、共有Duty）
- [ ] 課金設定UI（アップグレード導線、プラン管理画面、解約フロー）

## v1.1以降で追加する機能（参考・再掲）

`07-roadmap.md` に記載の後回し機能。着手時期の判断材料として理由込みで再掲する。

| 機能 | 後回しの理由 |
|---|---|
| Duty の DnD 並び替え | 無料プランDuty5件までなので作成順表示で当面代替可（DBの`sortOrder`列自体はMVPから保持） |
| 月次カレンダーヒートマップ | 獲得ループの本体は週次サマリー＋シェア画像で、ヒートマップは補助機能 |
| 通知のメールフォールバック | まずWeb Pushのみで配信し、iOS制約の影響を見てから判断 |
| 前夜通知（Duty単位） | 朝夜の基本2通が安定してから個別オプトインとして追加 |
| データエクスポート（JSON/CSV） | 初期ユーザー数を踏まえ優先度を下げる |
| メールアドレス変更 | 退会機能は維持するが変更系は後回し |
| 課金（Stripe連携・Proプラン制御） | MVPは無料プラン制約ロジックのみとし、決済連携自体を後回しにする |
