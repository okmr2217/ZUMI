## 概要

<!-- このPRで何をするか、1〜2行で -->

## 関連ドキュメント / Issue

<!-- 関連する docs/xx-*.md の章や Issue へのリンク。設計判断の根拠となった「なぜそう設計したか」の節があれば明記する -->

-

## 変更内容

<!-- 実装した内容を箇条書きで -->

-

## 影響範囲

<!-- 該当するものにチェック -->

- [ ] `apps/web`（アプリ本体）
- [ ] `apps/notify`（通知 Worker）
- [ ] `packages/db`（Drizzle スキーマ / マイグレーション）
- [ ] `packages/types`（期日計算ロジックなど共有型）
- [ ] インフラ / CI 設定

## スクリーンショット・動作確認

<!-- UI変更がある場合のみ。04-screens.md 冒頭の画面モック Artifact との対応（どのアートボードに対応するか）も記載 -->

## 確認事項

- [ ] `docs/` の該当章の設計理由と矛盾していない
- [ ] 明示的に非対応とされている要素（絵文字アイコン / ストリーク表示 / カスタムフィールド / `dueTime` / `YEARLY` 繰り返し）を誤って実装していない
- [ ] 期日計算ロジック（6種の繰り返しタイプ判定）に変更がある場合、`packages/types` の純粋関数を `web` / `notify` の双方が参照する形になっている
- [ ] D1（SQLite）の制約に合わせ、enum・Date・Json 相当のカラムを `text` + Drizzle の `$type<>()` で扱っている

## テスト

- [ ] `npm run lint`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] 手動確認: <!-- 確認した操作・画面 -->

## 備考

<!-- レビュアーに伝えたい懸念点、後回しにした事項など -->
