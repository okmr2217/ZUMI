# better-auth / Web Push（VAPID）/ Resend など、Cloudflare Workers の
# バインディング経由（`getCloudflareContext().env`）で読む実行時シークレットは
# ここではなく `.dev.vars`（`.dev.vars.example` 参照）で管理する。

# Sentry（ビルド時に process.env から読む。プロジェクト作成後に反映）
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_AUTH_TOKEN=
